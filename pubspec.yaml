name: attendance_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  camera: ^0.11.3
  cloud_firestore: ^6.1.1
  cloudinary: ^1.2.0
  envied: ^1.3.2
  firebase_auth: ^6.1.3
  firebase_core: ^4.3.0
  firebase_ml_model_downloader: ^0.4.0+5
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4
  flutter_map: ^8.2.2
  flutter_native_splash: ^2.4.7
  geolocator: ^14.0.2
  get: ^4.7.3
  get_storage: ^2.1.1
  google_fonts: ^6.3.3
  google_mlkit_face_detection: ^0.13.1
  image: ^4.7.2
  intl: ^0.20.2
  latlong2: ^0.9.1
  permission_handler: ^12.0.1
  tflite_flutter: ^0.12.1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  envied_generator: ^1.3.2
  build_runner: ^2.10.4

flutter:
  uses-material-design: true

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/attendance.png"